@{
    Layout = "_AdminPanel"; // Spécifie un layout différent pour le backoffice
}

@model library.Models.Emprunt

@{
    ViewData["Title"] = "Create";
}

<style>
    /* Add your vintage button style here */
    .vintage-button {
        background-color: #e0c9a6;
        color: #4c4c4c;
        padding: 8px 16px;
        text-decoration: none;
        border: 1px solid #4c4c4c;
        border-radius: 4px;
        display: inline-block;
        transition: background-color 0.3s;
    }

        .vintage-button:hover {
            background-color: #4c4c4c;
            color: #e0c9a6;
        }
</style>

<h1>Create</h1>

<h4>Emprunt</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LivreId" class="control-label"></label>
                @Html.DropDownList("LivreId", (SelectList)ViewBag.AvailableBooks, "Select a book", new { @class = "form-control" })
                <span asp-validation-for="LivreId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AbonneId" class="control-label"></label>
                @Html.DropDownList("AbonneId", (SelectList)ViewBag.AvailableAbonnes, "Select an abonne", new { @class = "form-control" })
                <span asp-validation-for="AbonneId" class="text-danger"></span>
            </div>


            <div class="form-group">
                <label asp-for="DateEmprunt" class="control-label"></label>
                <input asp-for="DateEmprunt" class="form-control" />
                <span asp-validation-for="DateEmprunt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DateRetour" class="control-label"></label>
                <input asp-for="DateRetour" class="form-control" />
                <span asp-validation-for="DateRetour" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary vintage-button" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="vintage-button">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
